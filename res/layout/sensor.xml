<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" 
    android:background="@drawable/bg_main_app">

    <Button android:id="@+id/sensor_open"
	    android:layout_width="100dp"
	    android:layout_marginTop="20dp"
	    android:layout_marginLeft="30dp"
    	android:layout_height="40dp"
    	android:text="@string/SensorOpen"/>
    
    <Button android:id="@+id/sensor_close"
	    android:layout_width="100dp"
	    android:layout_marginTop="20dp"
	    android:layout_marginLeft="30dp"
    	android:layout_height="40dp"
    	android:layout_toRightOf="@+id/sensor_open"
    	android:text="@string/SensorClose"/>

</RelativeLayout>